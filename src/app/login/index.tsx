import { Text, View } from "react-native";
import React, { Component } from "react";
import FormLogin from "@//components/formClient/login";

export default class Login extends Component {
  render() {
    return (
      <View>
        <FormLogin />
      </View>
    );
  }
}
